# -*- mode: snippet -*-
# name: rrspec
# key: rrspec
# --
require 'rails_helper'

RSpec.describe ${1:`(let ((fn (capitalize (file-name-nondirectory
				 (file-name-sans-extension
				 (or (buffer-file-name)
				     (buffer-name (current-buffer))))))))
	     (replace-regexp-in-string "_" "" (replace-regexp-in-string "_spec" "" fn) t t))`} do
  $0
end
